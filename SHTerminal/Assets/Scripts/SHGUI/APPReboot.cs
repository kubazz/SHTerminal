using System;
using UnityEngine;
using System.Collections;

public class APPReboot : SHGUIview
{
    public APPReboot()
    {
        //level = LevelSetup.GetLevelInfoByName(SaveManager.Instance.GetValue(SaveManager.LastPlayedLevel) as String);
        
    }
}

